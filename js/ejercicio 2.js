// Crea un vector con los numeros del 1 al 5 luego elimina el numero que esta en la segunda posicion 
// y luego muestra el vector

// Crear el vector

let vector = [1, 2, 3, 4, 5];

// Eliminar el número en la segunda posición

vector.splice(1, 1);

// Mostrar el vector

console.log(vector);